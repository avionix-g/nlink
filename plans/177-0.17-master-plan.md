---
to: nlink maintainers
from: nlink maintainers (post-0.16 cut retrospective, 2026-05-25)
subject: 0.17 cycle master plan — fix the issues surfaced during the 0.16 cycle
status: cycle complete (2026-05-25) — all sub-plans 🟢; workspace at 0.17.0 awaiting cut
target version: 0.17.0
parent: none — top-level for the 0.17 cycle (0.16 shipped 2026-05-25 as `v0.16.0`)
created: 2026-05-25
---

# Plan 177 — 0.17 cycle master

The 0.16 cycle shipped (`v0.16.0` tagged + both crates published
2026-05-25). The work itself was substantial; the *process* of
shipping it surfaced a coherent set of issues — most around
defensive coding patterns the lib should have but doesn't. This
plan catalogues them, ranks by user-visible severity, and
orchestrates the per-issue plans (170-176) that fix them.

## 1. Theme of the cycle

**"The hidden hang."** The integration-test CI run that gated
0.16's cut hung for 22 minutes on its first execution. Step B
+ C of the cut activation (Plan 167) localized the cause to a
single `recv_msg().await?` loop in `Connection::<Nftables>::send_batch`
that lacks both a sequence-number filter and a timeout. We shipped
0.16.0 with the 7 affected integration tests `#[ignore]`'d and
Plan 170 queued.

But the *pattern* — recv-loops that can hang forever if the
kernel response shape doesn't match expectations — is wider than
one function. **Plan 172** audits every recv-loop in the lib;
**Plan 171** introduces a default operation timeout so future
"hidden hangs" surface as `Error::Timeout` instead of silent
indefinite blocks; **Plan 174** wires `tracing-subscriber` into
the integration test harness so the next "hidden hang" comes
with debug logs.

Two unrelated UX papercuts also surfaced and rate their own
plans (**Plan 173**: parse-error `From`-impls; **Plan 175**:
release-cut tooling).

## 2. Sub-plans

| Plan | Title | Status | Outcome |
|------|-------|--------|---------|
| [170](170-nft-send-batch-hang-investigation-plan.md) | `send_batch` seq filter + end-seq termination | 🟢 | `dc4c103` — seq filter + end-seq term + `NLM_F_ACK` on `NFNL_MSG_BATCH_END`; 4 of 7 reconcile tests un-ignored. |
| [171](171-default-connection-timeout-plan.md) | Default 30s operation timeout on `Connection<P>` | 🟢 | `2a1251f` — `DEFAULT_OPERATION_TIMEOUT = 30s` on every constructor; opt-out via `.no_timeout()`. |
| [172](172-recv-loop-audit-plan.md) | Audit + harden every recv-loop | 🟢 | `ee2a75f` — 9 recv-loops routed through `self.with_timeout`; canonical shape documented in CLAUDE.md. |
| [173](173-parse-error-from-impls-plan.md) | `From<AddressParseError>` + `From<RouteParseError>` for `nlink::Error` | 🟢 | `e00e365` — additive; cleans up `examples/config/declarative.rs` ceremony. |
| [174](174-ci-observability-plan.md) | tracing-subscriber + modprobe completeness + IGNORED.md catalog | 🟢 | `64bb854` — `init_test_tracing()` auto-wired into `require_root!()`; `audit-ignored-tests` CI gate added. |
| [175](175-release-cut-tooling-plan.md) | `scripts/cut-release.sh` end-to-end orchestrator | 🟢 | `778dcf4` — pre-flight + CHANGELOG promotion + dry-runs + publish + GitHub release; CLAUDE.md "Publishing" rewritten. |
| [176](176-hardware-test-coverage-plan.md) | Hardware-only test coverage strategy doc (§3.3) | 🟢 | `8d24210` — `docs/release-validation-manual.md` + cut-script hook + CHANGELOG annotation convention. §3.1/§3.2 paths deferred to Plans 180/181. |
| [178](178-nftables-diff-body-bytes-false-positive-plan.md) | `NftablesDiff` body-bytes false-positive | 🟢 | `7549039` + `49455d2` — canonical `NFT_REG_x` discriminants + `#[repr(u32)]`, `normalize_tlv` (strips `NLA_F_NESTED`, sorts sibling attrs), `rules_to_delete` gained chain field. All 3 reconcile tests un-ignored + green. |
| [169 Phase 3](169-bottleneck-score-design.md) | `Bottleneck::score: f64` formula | 🟢 | `11078c5` — `drop_rate × 0.6 + backlog × 0.3 + error × 0.1`, saturating; 6 unit tests. |

Plan 178 was added mid-cycle (not in the original 170-176 + 169
roster) — surfaced when Plan 170's send_batch fix un-ignored
the 7 reconcile tests and 3 of them failed with a NEW
body-bytes-comparison bug. Plan 178 became the cycle's only
mid-cycle scope addition.

Total estimated focused-work: **~13 h** (per original 170-176
+ 169 estimate); actual landed work also includes Plan 178
(~3 h captured + fix iteration).

## 3. Sequencing rationale

1. **170 first** — un-ignores the 7 reconcile tests, restores
   the regression net for the rest of the cycle's work.
2. **171 next** — default timeout is independent of 170 but
   benefits from 170's lib changes being settled (touches the
   same socket layer). Without 171, every future Plan-170-class
   bug recurs as a silent hang.
3. **172 after 170 + 171** — the audit's recommended fix for
   each problem loop is "switch to the seq-filtered shape from
   170 and wrap in the default timeout from 171." So 170 + 171
   are prerequisites for 172's per-loop edits.
4. **173** — independent additive fix; can land anytime.
   Sequenced after the recv-loop work to keep the lib changes
   bundled.
5. **174** — would have made 170 easier to diagnose (debug
   logs from tracing-subscriber would have surfaced the hang
   point in the first CI run instead of the 3 push-watch-fix
   iterations). Land mid-cycle so 0.18's debug story is better.
6. **175** — release tooling. Apply to the 0.17 cut.
7. **176** — strategy doc; doesn't unblock anything; land at
   maintainer's leisure.
8. **169 Phase 3** — the design call deferred from 0.16. Land
   last so any related diagnostics work folds into the formula.

## 4. Themes that did NOT surface as 0.17 work

Out of scope for this plan:

- **Plan 152** (aya / Prometheus / OTel integration showcases) —
  remains deprioritized per the 0.16 cycle decision. No new
  signal indicates it should bubble up.
- **Public-API stability work** beyond what Plan 163 already
  did — `cargo-semver-checks` is enforced in CI, so semver
  drift is structurally blocked.
- **MSRV bump** — 0.16 raised MSRV to 1.95. No need to revisit
  until a 1.95 feature gains value the lib wants.
- **New kernel families** beyond DPLL / net_shaper — neither
  the macro stack nor any user request currently points at a
  next family.

## 5. Acceptance criteria

- [x] All sub-plans 🟢 (170-176, 178, 169 Phase 3 — 9 plans
      landed).
- [x] All 7 `nftables_reconcile::*` tests un-ignored — Plan
      170 un-ignored 4; Plan 178 un-ignored the remaining 3
      after fixing the body-bytes false-positive surfaced by
      170's fix.
- [x] `Connection::<P>` ships with a default 30s operation
      timeout (Plan 171); `.no_timeout()` opts out, `.timeout()`
      overrides.
- [x] No recv-loop in the lib can hang indefinitely on
      missing-DONE / wrong-seq kernel responses (Plan 172
      closed: 9 loops routed through `with_timeout`).
- [x] `nlink::Error: From<AddressParseError> + From<RouteParseError>`
      (Plan 173).
- [x] Integration test harness sets up `tracing-subscriber`
      with `RUST_LOG` env-driven filter (Plan 174).
- [x] `scripts/cut-release.sh` walks the cut sequence end-to-end
      with confirmations at irreversible steps (Plan 175).
- [x] Plan 176 documents the hardware-test coverage gap +
      proposed mitigation paths (Plans 180/181 sketched).
- [x] `Bottleneck::score()` ships with a documented formula
      (Plan 169 Phase 3).
- [x] CHANGELOG entries for every plan that landed (under
      `## [Unreleased]`, ready for the cut-script's promotion
      sed).
- [x] Migration guide for the breaking changes
      ([`docs/migration_guide/0.16.0-to-0.17.0.md`](../docs/migration_guide/0.16.0-to-0.17.0.md)
      — Register discriminants, `rules_to_delete` tuple shape,
      30s default timeout).
- [x] Workspace version bumped 0.16.0 → 0.17.0 (`49455d2`,
      forced by `cargo-semver-checks` flagging Plan 178's
      Register discriminant change as semver-major).

## 6. Process improvements baked in this cycle

Compared to 0.16:

- **CI surfaces issues earlier** — Plan 174's tracing-subscriber
  init means the next "hidden hang" comes with debug logs in CI
  immediately, no need for the 3-push iteration loop that
  diagnosed Plan 170 (Plan 167 §B + §C).
- **Release cut is scripted** — Plan 175's
  `scripts/cut-release.sh` makes the Plan 167 runbook
  executable, with checks for the dry-run-inversion edge case
  and the GitHub release body length.
- **Recv-loop hang class is closed** — Plans 170 + 171 + 172
  remove the pattern that caused Plan 170; the default timeout
  is the safety net that catches anything missed.

## 7. Risks — retrospective

How the original risks actually played out:

- **Plan 170 fix doesn't reproduce locally**: confirmed —
  needed 2 CI iterations to land. Mitigation (iterate on the
  draft PR) worked.
- **Plan 171 default-timeout choice**: 30s chosen, no flakes
  observed across the cycle. The pre-existing integration
  suite already used 30s as its explicit cap, so the default
  matched established practice. No callers needed `.no_timeout()`.
- **Plan 172 audit surfaces more bugs than 170**: actually it
  surfaced a different shape — Plan 178 (the body-bytes false-
  positive) was found by *un-ignoring* 170's tests and reading
  the failures, not by the audit itself. Plan 172 found 8 of
  9 loops were already structurally sound; only the timeout
  wrap was needed.

Newly-surfaced (post-plan):

- **Plan 178's Register discriminant change is semver-major** —
  `cargo-semver-checks` correctly flagged it, forcing the
  workspace bump to 0.17.0 (documented in CLAUDE.md /
  INDEX.md but had been deferred to "the first 0.17-breaking
  change lands"). Bump landed as part of Plan 178's closeout
  (`49455d2`).
- **DELRULE chain assumption was wrong** — `apply.rs` was
  passing `chain=""` on the assumption the kernel ignores
  `NFTA_RULE_CHAIN` when `NFTA_RULE_HANDLE` is supplied.
  Surfaced when Plan 178's diff fix correctly identified a
  delete (the test no longer hit the body-bytes false-positive).
  Fixed in 178 closeout: `rules_to_delete` tuple shape gained
  the chain name; second semver-major change in the cycle.

## 8. Follow-up plans (post-0.17)

Plans opened this cycle but explicitly out of scope for 0.17:

- **Plan 179** — diagnostics.rs `#[ignore]` → `require_root!()`
  migration. 12 tests catalogued in IGNORED.md as migration
  candidates by Plan 174; the bulk swap is a 0.18 deliverable.
- **Plan 180** (sketched in Plan 176 §8) — self-hosted hardware
  runner. Open when a downstream adopter signals critical-
  infrastructure use of the hardware-only features.
- **Plan 181** (sketched in Plan 176 §8) — vendor cloud lab
  cycle. Open if a hardware-touching regression escapes the
  manual checklist in a future cycle.

Other latent-but-unscoped items (carried over from the original
§8):

- **0.18 release tooling polish** — once 0.17 cut runs through
  `scripts/cut-release.sh`, expect minor refinements.
- **Per-protocol connection pools** (`ConnectionPool<Nftables>`,
  `<Generic>`, etc.) — Plan 159's generic pool already works
  via `connection_for_async`; the typed-per-protocol convenience
  variants are nice-to-have.
- **Kernel-version matrix CI** — currently bookworm-only. A
  multi-version matrix (Ubuntu 22/24 + Fedora 40/41) would
  have caught Plan 170 earlier. Cost vs. benefit unclear;
  revisit if 0.18 surfaces another kernel-specific bug.

## 9. Cut readiness

Status as of cycle close (2026-05-25):

- Workspace at 0.17.0 (`Cargo.toml`); both crates tracked.
- CHANGELOG `## [Unreleased]` populated with Breaking /
  Fixed / Added sections covering all 9 plans.
- Migration guide written:
  [`docs/migration_guide/0.16.0-to-0.17.0.md`](../docs/migration_guide/0.16.0-to-0.17.0.md).
- CI green on every commit in the cycle PR.
- `scripts/cut-release.sh 0.17.0` ready to run when the
  maintainer is.

Per the post-cut convention (CLAUDE.md "Publishing /
Plan-file cleanup"), the per-plan files under `plans/` get
deleted in a follow-up commit once the cut publishes. Keep
INDEX.md (rewrite for 0.18) + Plan 179 (still in flight) +
Plan 152 (parked).

End of plan.
